'use strict'

// ==========scroll on reveal=====================

const sr = ScrollReveal({
  origin: 'top',
  distance: '90px',
  duration: 2000,
  reset: true
})

// ==================scroll home===================

sr.ScrollReveal('.home', {})
